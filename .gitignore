# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Rendered media is regenerated by the pipeline, never versioned.
*.mp4
*.wav
*.mp3

# team-kill-quota ships as a worked example: its sprites, background,
# renderer, config and docs are tracked. Its renders, QA frames and raw
# source generations are not — and source/audio holds third-party music.
# See that project's ATTRIBUTION.md before reusing anything under it.
skills/pixel-short-studio/team-kill-quota/frames/
skills/pixel-short-studio/team-kill-quota/output/
skills/pixel-short-studio/team-kill-quota/source/

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
